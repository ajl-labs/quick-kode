import { moderateScale } from 'react-native-size-matters';

export const ThemeSpacings = {
  none: 0,
  xxs: moderateScale(2),
  xs: moderateScale(4),
  sm: moderateScale(8),
  md: moderateScale(12),
  lg: moderateScale(24),
  xl: moderateScale(32),
  xxl: moderateScale(40),
};
